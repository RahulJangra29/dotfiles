# Dotfiles

Minimal bash + zsh setup.

## Rebuild (WSL / Ubuntu)

```bash
sudo apt update
sudo apt install -y git zsh curl
git clone https://github.com/RahulJangra29/dotfiles.git
cd dotfiles
./install.sh
